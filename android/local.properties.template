## 本文件不应签入版本控制系统，包含本地专属配置。
##
## 复制为 local.properties 后填入真实值。

# Android SDK 路径（仅 Gradle 使用）
sdk.dir=/home/rd/Android/Sdk

# === Release 签名配置 ===
# 密钥库文件路径
KEYSTORE_FILE=../voice_note-release.keystore
# 密钥库密码
KEYSTORE_PASSWORD=
# 密钥别名
KEY_ALIAS=voice_note
# 别名密码
KEY_PASSWORD=

## 生成签名文件

keytool -genkey -v \
    -keystore voice_note-release.keystore \
    -alias voice_note \
    -keyalg RSA -keysize 2048 -validity 10000 \
    -storepass 'your_password' \
    -keypass 'your_password' \
    -dname "CN=voicenote, OU=dev, O=voicenote, L=Beijing, ST=Beijing, C=CN"
